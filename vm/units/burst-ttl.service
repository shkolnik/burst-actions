[Unit]
Description=burst hard TTL cap

[Service]
Type=oneshot
# Unconditional of runner/job state: the hard cap that bounds worst-case
# billing even if every other cleanup layer fails.
ExecStart=/opt/burst/burst-ttl-check.sh
